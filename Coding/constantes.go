package main

import (
	"fmt"
	// "math"
)

func main() {
	
	const Pi = 3.14 // definindo uma constante - seu valor não se altera nem redefine

	//var Numero = math.Max(3, 5) // retorna o maior entre dois números

	 fmt.Printf("Valor: %v", Pi)
	// fmt.Printf("Valor: %v", Numero)
}
